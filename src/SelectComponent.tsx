import Select from "react-select";

// this import is included in vendor chunk, instead of SelectComponent chunk
// import { Select } from "react-lib";

const SelectComponent = () => {
  return <Select />;
};

export default SelectComponent;
